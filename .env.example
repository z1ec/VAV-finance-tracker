SECRET_KEY=
ADMIN_USERNAME=admin
ADMIN_PASSWORD=
USER1_USERNAME=
USER1_PASSWORD=
USER2_USERNAME=
USER2_PASSWORD=
DB_PATH=/data/expenses.db
TZ=Europe/Budapest
# localhost -> plain HTTP on :80 for local dev.
# A real domain -> Caddy gets a Let's Encrypt cert and serves HTTPS on :8443
# (not :443 — that port is reserved for another service). See README.
DOMAIN=localhost
